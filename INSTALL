Requirements:

    setuptools
    google-protobuf with python3 support, eg. https://github.com/malthe/google-protobuf

Run:

    $ python3 setup.py build
    $ sudo python3 setup.py install

Or using pip for git:

    $ pip install 'git+ssh://git@bitbucket.org/evolutek/python-cellaserv.git#egg=python-cellaserv'
